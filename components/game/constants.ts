/** Arena half-extent on X/Z (playable floor is ARENA_SIZE * 2). */
export const ARENA_SIZE = 8;

export const MAX_SPEED = 5.8;
export const ACCEL = 22;
export const FRICTION = 16;

export const PLAYER_RADIUS = 0.42;
export const PLAYER_HEIGHT = 2.6;

export const CAMERA_POS: [number, number, number] = [0, 11.5, 13.5];
export const CAMERA_TARGET: [number, number, number] = [0, 0.4, 0];
